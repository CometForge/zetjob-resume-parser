#!/usr/bin/env bash
set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "[pre-commit] gitleaks not found. Install: https://github.com/gitleaks/gitleaks" >&2
  exit 1
fi

gitleaks protect --staged --verbose
